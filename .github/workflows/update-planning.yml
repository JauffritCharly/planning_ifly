name: Génération automatique du planning

on:
  workflow_dispatch: # Permet de lancer manuellement le workflow

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 👈 Ajoutez cette ligne pour les permissions d'écriture

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 # 👈 Utilisez la version la plus récente

      - name: Set up Python
        uses: actions/setup-python@v5 # 👈 Utilisez la version la plus récente
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pdfplumber

      - name: Run planning script
        run: python planning_parser.py

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
      - name: Commit and push JSON
        run: |
          git add planning.json
          git commit -m "Mise à jour automatique du planning" || true
          git push